name: Troll
version: ${project.version}
api-version: 1.13
author: einTosti
main: com.eintosti.troll.Troll
description: Time to have a blast!

commands:
  troll:
    description: Time to have a blast!
    usage: /<command>

permissions:
  troll.use:
    description: Not everybody can have all of the fun...
    default: op
